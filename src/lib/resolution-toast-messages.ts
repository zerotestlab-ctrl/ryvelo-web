/** User-visible copy — shared by server action responses and client toasts (Sonner). */
export const RESOLUTION_APPROVED_TITLE = "Resolution approved";

/** Primary success after approve + execute (Paystack link stored on resolution). */
export const RESOLUTION_APPROVED_PAYMENT_READY =
  "Resolution approved — payment link ready";

/** When approve is recorded but email / workflow step failed. */
export const APPROVED_EMAIL_STEP_SKIPPED_TITLE =
  "Approved — email step skipped due to error";
